# Robolectric runtime SDK. Pinned to 35 (Android 15): it runs on Java 17 (the bundled
# Android Studio JBR), whereas Robolectric's API-36 image requires Java 21 and fails the
# sandbox locally. 35 is ample for these tests. Bump once the toolchain is on JDK 21+.
sdk=35
